/**
 * Implementation of hook_menu().
 */
function <?php print $basename; ?>_menu() {
	\$items = array();

<?php /* Note: Using $0 instead of $1 makes the snippet tabbing stop here. */?>
  ${0:// Type '\$item⇥' to create a new menu item.}

	return \$items;
}
